<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC '-//ibatis.apache.org//DTD Sql Map 2.0//EN'
	'http://ibatis.apache.org/dtd/sql-map-2.dtd'>
<sqlMap>
	<typeAlias alias="hotel" type="item.hotel.HotelVO" />
	<!-- 리절트 맵 정의 -->
	<resultMap id="boardhotel" class="hotel">
		<result property="no" column="no" />
		<result property="name" column="name" />
		<result property="price" column="price" />
		<result property="content" column="content" />
		<result property="country" column="country" />
		<result property="region" column="region"/>
		<result property="readcount" column="readcount"/>
		<result property="image1" column="image1" />
		<result property="image2" column="image2" />
		<result property="image3" column="image3" />
		<result property="reg_date" column="reg_date" />
		<result property="tel" column="tel" />
		<result property="checkin" column="checkin" />
		<result property="checkout" column="checkout" />
		<result property="maxnum" column="maxnum" />
		<result property="grade" column="grade" /> 
	</resultMap>
	
	<!-- 공통으로 사용할 select 절 정의 -->
		<sql id="select-allH">
			SELECT * FROM HOTEL 
		</sql>
		
		<!-- 공통으로 사용할 where-no 절 정의 -->
	 	<sql id="where-noH">
			WHERE no = #no#
		</sql>
	
		<!-- select 쿼리문 정의 -->
		<select id="selectAllH" resultMap="boardhotel">
			<include refid="select-allH" />
			ORDER BY NO DESC
		</select>
		
		<select id="selectOneH" resultMap="boardhotel" parameterClass="int">
			<include refid="select-allH" />
			<include refid="where-noH" />
		</select>
		
		<select id="selectLastNoH" resultClass="hotel">
				select max(no) as no from hotel
		</select>
		
		<insert id="insertHotel" parameterClass="hotel">
			insert into hotel(no, name, price, content, country, region, readcount, image1, image2, image3, reg_date, tel,checkin,checkout,maxnum,grade)
			values(seq_hotelnum.nextval, #name#, #price#, #content#, #country#, #region#, 0, #image1#, #image2#, #image3#, sysdate, #tel#,#checkin#,#checkout#,#maxnum#,#grade#)	
		</insert>


</sqlMap>